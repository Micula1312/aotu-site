<head>
  ...
  <link rel="stylesheet" href={`${import.meta.env.BASE_URL}css/terminal.css`} />
  <title><slot name="head-title" /></title>
</head>
<body>
  <main class="layout container">
    <Sidebar />
    <section class="content">
      <slot />
      <audio id="bg-audio" preload="auto" playsinline loop muted>
        <source src={`${import.meta.env.BASE_URL}assets/audio/soundscape.mp3`} type="audio/mp3" />
      </audio>
    </section>
  </main>

  <!-- scripts from /public: add is:inline and use BASE_URL -->
  <script src={`${import.meta.env.BASE_URL}js/core.js`} is:inline></script>
</body>
